// 'close' dismisses the palette from any depth (VS Code style); 'pop' goes back
// one page per press and only closes at the root (Raycast style).
export type CommandPaletteEscapeBehavior = 'close' | 'pop';
